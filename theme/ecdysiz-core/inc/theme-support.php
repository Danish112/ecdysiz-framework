<?php
/**
 * Theme supports — add_theme_support() declarations.
 *
 * Single responsibility: declare WordPress theme supports, image sizes, menus.
 * Populated in Step 6.
 *
 * @package Ecdysiz_Core
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Step 6: title-tag, post-thumbnails, html5, responsive-embeds,
// custom-logo, editor-styles, align-wide, menu locations.
