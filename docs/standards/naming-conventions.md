# Naming Conventions

**Status:** Stub (Step 1). Populated in Step 2.

| Surface | Prefix | Example |
|---|---|---|
| CSS custom property | `--ecz-` | `--ecz-color-surface` |
| CSS class | `.ecz-` | `.ecz-btn` |
| PHP function | `ecz_` | `ecz_enqueue_assets()` |
| PHP class | `Ecdysiz_` | `Ecdysiz_Cascade` |
| PHP constant | `ECZ_` | `ECZ_VERSION` |
| JS global | `ecz` | `window.ecz` |

Enforced by linters where possible, by review otherwise.