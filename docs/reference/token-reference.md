# Token Reference

**Status:** Stub (Step 1). Populated in Step 4.

The complete reference for every token in `src/tokens/tokens.json` —
its category, role, what it expresses, and where it's consumed.

Categories (per Phase 1.3):

1. color (primitives + semantics)
2. typography
3. spacing
4. radius
5. shadow
6. border
7. motion
8. z-index
9. opacity
10. layout / container
11. breakpoint