# Ecdysiz Framework Documentation

## Structure

- `architecture/` — system design and rules
- `standards/` — coding, naming, git, versioning
- `reference/` — token reference, component contract template
- `decisions/` — Architecture Decision Records (ADRs)

## Where to start

1. `architecture/overview.md` — the framework's structure and goals
2. `architecture/token-authority.md` — the single-source-of-truth model
3. `architecture/cascade-strategy.md` — how `@layer client` beats Elementor
4. `architecture/component-delivery.md` — theme-delivered vs Elementor-delivered
5. `decisions/` — read every ADR

Populated progressively across Steps 1–11.